/*
 A package for metrics collection and reporting.

 Most calls read /proc and are Linux-only (for now, at least). Some call
 POSIX functions to collect data, so you need a C compiler to build this
 package.
*/
package gollector_metrics
